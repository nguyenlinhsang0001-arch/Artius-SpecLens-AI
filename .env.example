# Copy thành .env.local để test /api/analyze bằng `vercel dev` ở máy.
# Khi deploy thật: đặt biến này trong Vercel Project Settings -> Environment Variables
# (KHÔNG commit file .env.local hay key thật vào git).
ANTHROPIC_API_KEY=sk-ant-api03-...
